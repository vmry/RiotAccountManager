# Security

Do not publish Riot API keys, Riot login passwords, exported `.ramx` backups, or local application data in issues or commits.

The application stores passwords and the Riot API key using the operating system credential store. Metadata and cached Riot statistics are stored separately without passwords.

If you discover a security issue, report it privately to the repository owner rather than opening a public issue containing sensitive details.
